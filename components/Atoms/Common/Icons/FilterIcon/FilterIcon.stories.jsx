import React from 'react';

import {FilterIcon} from './index';

export default {
    title: 'Atoms/Common/Icons/FilterIcon',
    component: FilterIcon,
    argTypes: {},
};

const Template = (args) => <FilterIcon {...args} />;

export const Default = Template.bind({});
Default.args = {};