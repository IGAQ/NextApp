import React from 'react';

import {ContentButton} from './index';

export default {
    title: 'Atoms/Post/ContentButton',
    component: ContentButton,
    argTypes: {},
};

const Template = (args) => <ContentButton {...args} />;

export const Default = Template.bind({});
Default.args = {};

