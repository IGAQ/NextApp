import React from 'react';

import {RadioGroup} from './index';

export default {
    title: 'Molecules/Common/RadioGroup',
    component: RadioGroup,
    argTypes: {},
};

const Template = (args) => <RadioGroup {...args} />;

export const Default = Template.bind({});
Default.args = {};
